@model MBBS.Dashboard.web.Models.DashboardViewModel

@{
    ViewBag.Title = "Dashboard";
}

<h2>Dashboard</h2>

@if (Model == null || Model.KpiData == null)
{
    <p>No dashboard data available.</p>
}
else
{
    <h3>Key Performance Metrics</h3>
    <!-- Categorized KPIs Section -->
    <div class="row">
        <!-- Key Performance Metrics Section -->
        <div class="col-md-6">
            <h3>Key Performance Metrics</h3>
            <ul class="list-group">
                <li class="list-group-item"><strong>Total Users:</strong> @Model.KpiData.TotalUsers</li>
                <li class="list-group-item"><strong>Total Coursera Users:</strong> @Model.KpiData.TotalCourseraUsers</li>
                <li class="list-group-item"><strong>Total Cognito Users:</strong> @Model.KpiData.TotalCognitoUsers</li>
                <li class="list-group-item"><strong>Total Google Forms Users:</strong> @Model.KpiData.TotalGoogleFormsUsers</li>
                <li class="list-group-item"><strong>Mentor Completion Rate:</strong> @Model.KpiData.MentorCompletionRate.ToString("P2")</li>
                <li class="list-group-item"><strong>Phone Number Provision Rate:</strong> @Model.KpiData.PhoneNumberProvisionRate.ToString("P2")</li>
                <li class="list-group-item"><strong>Total Mentoring Sessions:</strong> @Model.KpiData.TotalMentoringSessions</li>
            </ul>
        </div>

        <!-- Platform Data Section -->
        <div class="col-md-6">
            <h3>Platform Data</h3>
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Quick Platform Overview</h5>
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item">Coursera Users: @Model.KpiData.TotalCourseraUsers</li>
                        <li class="list-group-item">Cognito Users: @Model.KpiData.TotalCognitoUsers</li>
                        <li class="list-group-item">Google Forms Users: @Model.KpiData.TotalGoogleFormsUsers</li>
                    </ul>
                    <div class="mt-3">
                        <a href="@Url.Action("ViewDataByPlatform", new { platformId = 1 })" class="btn btn-sm btn-outline-primary">Coursera Details</a>
                        <a href="@Url.Action("ViewDataByPlatform", new { platformId = 2 })" class="btn btn-sm btn-outline-primary">Cognito Details</a>
                        <a href="@Url.Action("ViewDataByPlatform", new { platformId = 3 })" class="btn btn-sm btn-outline-primary">Google Forms Details</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

                    <h4 class="mt-3">Specialization Distribution</h4>
                    <ul class="list-group">
                        @{
                            var specializationItems = Model.GoogleCertificationKPIs.SpecializationDistribution
                            .AsEnumerable()
                            .OrderByDescending(pair => pair.Value)
                            ;
                        }
                        @foreach (var kvp in specializationItems)
                        {
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                @kvp.Key
                                <span class="badge bg-primary rounded-pill">@kvp.Value</span>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </div>

        <!-- Mentoring Program -->
        <div class="col-md-4">
            <h3>Intended Major Distribution</h3>
            @if (Model.KpiData.IntendedMajorDistribution != null && Model.KpiData.IntendedMajorDistribution.Any())
            {
                <ul class="list-group">
                    @foreach (var kvp in Model.KpiData.IntendedMajorDistribution)
                    {
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            @kvp.Key
                            <span class="badge bg-primary rounded-pill">@kvp.Value</span>
                        </li>
                    }
                </ul>
            }
            else
            {
                <p>No intended major data available.</p>
            }
        </div>
        <div class="col-md-4">
            <div class="card">
                <div class="card-header bg-info text-white">
                    <h3>Scholarship Application Program</h3>
                </div>
                <div class="card-body">
                    <ul class="list-group">
                        <li class="list-group-item"><strong>Total Applications:</strong> @Model.ScholarshipApplicationKPIs.TotalApplications</li>
                        <li class="list-group-item"><strong>Phone Provision Rate:</strong> @Model.ScholarshipApplicationKPIs.PhoneNumberProvisionRate.ToString("P2")</li>
                        <li class="list-group-item"><strong>Average GPA:</strong> @Model.ScholarshipApplicationKPIs.AverageGPA.ToString("F2")</li>
                    </ul>

                    <h4 class="mt-3">Top Intended Majors</h4>
                    <ul class="list-group">
                        @{
                            var sortedMajors = Model.ScholarshipApplicationKPIs.IntendedMajorDistribution
                            .AsEnumerable()
                            .OrderByDescending(pair => pair.Value)
                            ;

                        }
                        @foreach (var kvp in sortedMajors)
                        {
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                @kvp.Key
                                <span class="badge bg-info rounded-pill">@kvp.Value</span>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </div>
    </div>

    <!-- Top Mentors Section -->
    <div class="row mt-4">
        <div class="col-md-6">
            <h3>Top Mentors</h3>
            @if (Model.KpiData.TopMentors != null && Model.KpiData.TopMentors.Any())
            {
                <ol class="list-group list-group-numbered">
                    @foreach (var mentor in Model.KpiData.TopMentors)
                    {
                        <li class="list-group-item">@mentor</li>
                    }
                </ol>
            }
            else
            {
                <p>No top mentors data available.</p>
            }
        </div>
    </div>

    <!-- Detailed Platform Data Section -->
    @if (Model.PlatformData != null)
    {
        <div class="mt-4">
            <h3>Platform-Specific Data</h3>

            @if (Model.PlatformId == 1) // Coursera
            {
                <h3>Coursera Reports</h3>

                <ul class="nav nav-tabs" id="courseraReportsTab" role="tablist">
                    <li class="nav-item" role="presentation">
                        <button class="nav-link active" id="membership-tab" data-bs-toggle="tab" data-bs-target="#membership" type="button" role="tab">Membership Report</button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link" id="location-tab" data-bs-toggle="tab" data-bs-target="#location" type="button" role="tab">Location Report</button>
                    </li>
                    <li class="nav-item" role="presentation">
                        <button class="nav-link" id="usage-tab" data-bs-toggle="tab" data-bs-target="#usage" type="button" role="tab">Usage Report</button>
                    </li>
                </ul>

                <div class="tab-content" id="courseraReportsTabContent">
                    <!-- Membership Report -->
                    <div class="tab-pane fade show active" id="membership" role="tabpanel">
                        <table class="table table-striped mt-3">
                            <thead>
                                <tr>
                                    <th>Name</th>
                                    <th>Email</th>
                                    <th>Program</th>
                                    <th>Enrolled Courses</th>
                                    <th>Completed Courses</th>
                                    <th>Status</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var report in Model.CourseraMembershipReports)
                                {
                                    <tr>
                                        <td>@report.Name</td>
                                        <td>@report.Email</td>
                                        <td>@report.ProgramName</td>
                                        <td>@report.EnrolledCourses</td>
                                        <td>@report.CompletedCourses</td>
                                        <td>@report.MemberState</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>

                    <!-- Location Report -->
                    <div class="tab-pane fade" id="location" role="tabpanel">
                        <table class="table table-striped mt-3">
                            <thead>
                                <tr>
                                    <th>Location City</th>
                                    <th>Current Members</th>
                                    <th>Current Learners</th>
                                    <th>Total Enrollments</th>
                                    <th>Completed Courses</th>
                                    <th>Avg Progress</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var report in Model.CourseraPivotLocationCityReports)
                                {
                                    <tr>
                                        <td>@report.LocationCity</td>
                                        <td>@report.CurrentMembers</td>
                                        <td>@report.CurrentLearners</td>
                                        <td>@report.TotalEnrollments</td>
                                        <td>@report.TotalCompletedCourses</td>
                                        <td>@(report.AverageProgress?.ToString("P0") ?? "N/A")</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>

                    <!-- Usage Report -->
                    <div class="tab-pane fade" id="usage" role="tabpanel">
                        <table class="table table-striped mt-3">
                            <thead>
                                <tr>
                                    <th>Name</th>
                                    <th>Course</th>
                                    <th>Progress</th>
                                    <th>Completed</th>
                                    <th>Learning Hours</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var report in Model.CourseraUsageReports)
                                {
                                    <tr>
                                        <td>@report.Name</td>
                                        <td>@report.Course</td>
                                        <td>@(report.OverallProgress?.ToString("P0") ?? "N/A")</td>
                                        <td>@report.Completed</td>
                                        <td>@report.EstimatedLearningHours</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            }
            else if (Model.PlatformId == 2) // Cognito
            {
                <table class="table table-striped mt-3">
                    <thead>
                        <tr>
                            <th>First Name</th>
                            <th>Last Name</th>
                            <th>Phone</th>
                            <th>Intended Major</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.PlatformData)
                        {
                            <tr>
                                <td>@((item as dynamic).Name_First)</td>
                                <td>@((item as dynamic).Name_Last)</td>
                                <td>@((item as dynamic).Phone)</td>
                                <td>@((item as dynamic).IntendedMajor)</td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else if (Model.PlatformId == 3) // Google Forms
            {
                <table class="table table-striped mt-3">
                    <thead>
                        <tr>
                            <th>Mentor</th>
                            <th>Mentee</th>
                            <th>Date</th>
                            <th>Method of Contact</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.PlatformData)
                        {
                            <tr>
                                <td>@((item as dynamic).Mentor)</td>
                                <td>@((item as dynamic).Mentee)</td>
                                <td>@(((DateTime?)(item as dynamic).Date)?.ToString("d") ?? "N/A")</td>
                                <td>@((item as dynamic).MethodOfContact)</td>
                            }
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }

    <!-- Activity Logs Section -->
    <div class="mt-4">
        <h3>Activity Logs</h3>
        @if (Model.ActivityLogs != null && Model.ActivityLogs.Any())
        {
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Timestamp</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var log in Model.ActivityLogs)
                    {
                        <tr>
                            <td>@log.Timestamp.ToString("g")</td>
                            <td>@log.Action</td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <p>No activity logs found.</p>
        }
    </div>
}
